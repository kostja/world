class Feed(object):
    def __init__(name, no, filters):
        self.name = name
        self.no = no
        self.filters = filters
    def blacklists(emitter):
# the goal of a good blacklist filter is to reduce the amount
# of feeds which pass the blacklist by 90%.
# Assuming every filter is equally selective, and has probability
# if selecting  
# so, take 90%, divide by the number of list types, get and
# for each list insert 
        pass
    def caps:
        pass
    def limits:
        pass

