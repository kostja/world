class Subaccount(Filter):
    def __init__(publisher):

        self.prefix = ''
        for ch in publisher:
            if ch.lower() not in 'aeiou':
                self.prefix.append(ch.lower())
                if len(self.prefix) > 5: break;
# Up to 100 subaccounts per publisher
        self.total = random.uniform() * 100


            
        self.prefix = ''.join([ch.lower() 
        self.
        
    def value():

